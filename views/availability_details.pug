extends layout

block content
  h1 #{title}
    div
      h3 Name: #{tutorName}
      h5 Calendar type: #{calendarType}
      h2 some calendar data
      p #{JSON.stringify(availability)}

  if errors
   ul
    for error in errors
     li!= error.msg